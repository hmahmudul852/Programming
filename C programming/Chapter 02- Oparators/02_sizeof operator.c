#include<stdio.h>
int main()
{
    int w;
    float x;
    double y;
    char z;


    printf("size of an int = %d\n",sizeof(w));
    printf("size of an float = %d\n",sizeof(x));
    printf("size of an double = %d\n",sizeof(y));
    printf("size of an char = %d",sizeof(z));
    return 0;
}
